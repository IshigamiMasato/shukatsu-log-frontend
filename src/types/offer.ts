export type Offer = {
    offer_id: number,
    apply_id: number,
    offer_date: string,
    salary: number|null,
    condition: string|null
    memo: string|null,
    created_at: string|null,
    updated_at: string|null,
}
